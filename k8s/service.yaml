apiVersion: v1
kind: Service
metadata:
  name: islands
  namespace: islands
  labels:
    app.kubernetes.io/name: islands
spec:
  type: ClusterIP
  ports:
    - name: mcp
      port: 8080
      targetPort: mcp
      protocol: TCP
    - name: webhook
      port: 9000
      targetPort: webhook
      protocol: TCP
  selector:
    app.kubernetes.io/name: islands
---
# Optional: Ingress for external webhook access
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: islands-webhook
  namespace: islands
  labels:
    app.kubernetes.io/name: islands
  annotations:
    kubernetes.io/ingress.class: nginx
    # cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  rules:
    - host: islands-webhook.example.com  # Replace with your domain
      http:
        paths:
          - path: /webhook
            pathType: Prefix
            backend:
              service:
                name: islands
                port:
                  number: 9000
  # tls:
  #   - hosts:
  #       - islands-webhook.example.com
  #     secretName: islands-webhook-tls
